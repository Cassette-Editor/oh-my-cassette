Tiny media fixtures generated with ffmpeg (see tests/test_prepare.py for the commands). They exist so
the import pipeline can be exercised without network access: `tone.wav` (0.5 s, 8 kHz mono),
`slide.png` (64x36) and `clip.mp4` (1 s, 160x90 @ 25 fps, H.264 + AAC).
